import { render, screen } from '@testing-library/react';
import { BrowserRouter } from 'react-router-dom';
import Account from './Account';

test("Accounts are properly rendered", () => {
    // Arrange
    // Act
    // Assert
});
